# 微博爬虫 (Weibo Crawler) - 增强版

本仓库基于 [dataabc/weibo-crawler](https://github.com/dataabc/weibo-crawler) 进行二次开发。

## 🌟 新增特性：图片发布时间对齐 (EXIF 写入)

在原版基础上，本仓库增强了图片管理功能，支持将微博的**发布时间**直接写入图片的元数据中。

* **智能排序支持**：在下载图片时，程序会自动获取微博的**发布时间**，并将其写入图片内部的 **EXIF (DateTimeOriginal)** 字段。
* **相册完美兼容**：将下载的图片导入手机相册、Google Photos 或 macOS Photos 时，图片将按照**微博实际发布的时间**进行排列，彻底解决因下载顺序导致的图片乱序问题。
* **格式自动转换**：系统会自动将微博的时间字符串转换为符合 EXIF 标准的规范格式。

---

## 🚀 配置说明

仓库已预设相关配置，您可以直接在 `config.json` 中按需开关此功能。

### 配置项：`sort_by_created_time`

| 参数值 | 说明 |
| --- | --- |
| **`1`** | **开启 (默认)**。下载图片时自动将微博发布时间写入 EXIF 信息。 |
| **`0`** | **关闭**。保持原版逻辑，不修改图片元数据。 |

> **技术说明**：
> 1. 本功能依赖 `piexif` 库（已包含在 `requirements.txt` 中）。
> 2. 根据 EXIF 规范，该功能目前主要支持 `.jpg` 和 `.jpeg` 格式的图片。
>
>

---

## 📝 致谢

感谢 [dataabc/weibo-crawler](https://github.com/dataabc/weibo-crawler) 提供的核心爬取逻辑。
